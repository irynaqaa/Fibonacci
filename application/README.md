# Application
This is a Spring Boot application that provides a RESTful API for managing transactions.

## Requirements
* Java 11
* Maven 3.6.0
* Spring Boot 2.5.0

## Usage
1. Clone the repository
2. Build the project using Maven
3. Run the application using Spring Boot
4. Use a tool like Postman to test the API endpoints
