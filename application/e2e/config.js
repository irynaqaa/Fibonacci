export const config = {
  verbose: true,
  grief: true,
  specs: './e2e/*.spec.js',
  cleanup: true,
  // setup: './e2e/init.js',
  // teardown: './e2e/cleanup.js',
  // timeout: 10000,
  // retries: 3,
};
