apiVersion: v1
kind: Secret
metadata:
  name: ima-secrets
type: Opaque
data:
  DATABASE_URL: <base64_encoded_database_url>
  JWT_SECRET_KEY: <base64_encoded_jwt_secret_key>
  # Add any other sensitive information here, encoded in base64
